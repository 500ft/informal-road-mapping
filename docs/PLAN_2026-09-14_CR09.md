# Plan for 2026-09-14 — CR-09: preserve corridor geometry in the exported path

Status: **revised plan for owner review; implementation has not started**.
Revision date: **2026-09-15**, reviewing the September 14 proposal.
This document supersedes [PR #17](https://github.com/500ft/informal-road-mapping/pull/17)
at `bb183263991bd644ab98fac1a5a2b858638594f3`. Review and merge this replacement
instead of #17. The build follows the merged version of this document; this
docs-only PR does not approve, implement, or validate the proposed algorithm.

Reviewed base: `main` at `9fa31ffc42d26c64f4fd278ee214fa1865fcdc2f` (PRs #15/#16).
Plan branch: `plan/cr09-reviewed-20260914`. Proposed build branch:
`task/cr09-path-export-20260914`, created from main after this plan merges.
Record the actual build base if main advances and check whether these assumptions
still hold. Task status authority: [SPRINT_TASKS.csv](SPRINT_TASKS.csv).
Editing convention: [AGENTS.md](../AGENTS.md). Research design: [design.md](design.md).

## Scope and measured defect

Deliver one connected pixel-space polyline for each **already accepted** component,
and make GeoJSON, line scoring, and the synthetic plot use the same geometry.
Candidate selection, IDs, order, component statistics, reference centerlines,
and all Phase-1 settings stay unchanged. This is a bounded synthetic representation
experiment; real imagery still depends on CR-08 and the Phase-1 gate.

The committed [v3 stress record](https://github.com/500ft/informal-road-mapping/blob/9fa31ffc42d26c64f4fd278ee214fa1865fcdc2f/results/extractor_stress_cases.json) separates
component detection from exported-line accuracy. Values below are rounded for
reading; comparisons use the full-precision record.

| Case | Component recall | Chord recall | Chord precision |
| --- | ---: | ---: | ---: |
| low_snr | 1.0000 | 0.1055 | 0.1107 |
| tight_curve | 1.0000 | 0.2452 | 0.2636 |
| demo_reference | 0.9230 | 0.4448 | 0.4518 |
| faint_corridor | 0.9284 | 0.9375 | 1.0000 |
| wide_corridor | 1.0000 | 1.0000 | 1.0000 |
| gradient_background | 1.0000 | 1.0000 | 0.9014 |

Source chain: [extract.py](../analysis/catanroads/extract.py) →
[to_geojson](../analysis/catanroads/extract.py),
[stress scoring](../analysis/catanroads/stress_cases.py), and
[demo plotting](../analysis/demo_synthetic.py). Each currently reads `endpoints_px`.
The internal component may follow a curve while its two-point export cuts across it.

## Critique of #17 and decisions resolved here

| Gap in #17 | Consequence | Replacement decision |
| --- | --- | --- |
| Requires all three straight cases to “keep” recall 1.00 | Faint recall is already 0.9375; the target misstates the baseline | Use explicit per-case non-regression criteria below |
| Treats two sweeps as mutually farthest endpoints | Pixel graphs have cycles; the tree-diameter guarantee does not apply | Call this an endpoint heuristic, define it, test its limits, and use a feasibility checkpoint |
| Gives edge cost as `1 / edt(pixel)` | The edge direction, diagonal length, and crop boundary are unspecified | Symmetric geometric edge costs and a padded component crop |
| Calls the weighted path a medial axis | A shortest path biased toward interior pixels need not be a medial axis or a road centerline | Say “interior-biased representative path”; prove performance on the named fixtures |
| Samples every four pixels | New connecting segments can leave a narrow bend or cross background | Keep every path vertex initially; simplification is deferred |
| Leaves the old numeric demo record as the only geometry check | A changed or broken polyline can escape the old endpoint-only assertions | Preserve the legacy record and add path assertions against v4 |
| Moves directly to new observed test values | Re-recording any output could turn a failed experiment green | Freeze acceptance criteria first; retain chord controls alongside path tests |
| Scores a candidate field without checking actual GeoJSON | Scoring can improve while a sibling exporter still emits chords | One coordinate selector, plus an export-to-scoring equality test |
| Omits legacy-input and malformed-path behavior | Existing endpoint-only callers break, or invalid new geometry silently falls back | Fallback only when `path_px` is absent; reject malformed present paths |
| Leaves length and width semantics implicit | Consumers may mistake chord extent for polyline length or road width | Add `path_length_px`; document all retained legacy scalars |
| Makes the remaining-failure taxonomy optional | The deliverable can conceal known limitations | Include a short mandatory limitations section in the build evidence |
| Overwrites the result record in the verification block | The CLI currently omits schema metadata; a failed redirect can truncate the record | Fix complete record generation, stage output in a temporary file, validate, then replace |

The three review choices in #17 are resolved as proposed design decisions:
**add `path_px` beside the chord; retain all vertices; ship the limitations note
with the build**. Merging this plan accepts those choices. No separate decision
about sampling, dependencies, or Task 2 is required during implementation.

## Ladder walk and algorithm contract

Reuse `label_candidates` and the current acceptance loop. Route only accepted
components. Keep NumPy/SciPy as the core dependencies; do not implement another
shortest-path solver, add a graph framework, or expand the `full` extra.

Proposed private helper in `analysis/catanroads/extract.py`:
`_component_path_px(mask: np.ndarray) -> list[list[float]]`. `mask` is the tight
boolean crop of one accepted connected component. The helper returns crop-local
`[x, y]` pixel centers; the caller adds the crop origin once.

1. Pad the crop with one background pixel on every side before computing
   `scipy.ndimage.distance_transform_edt`; remove the padding for node values.
   This also defines behavior at image borders. EDT measures distance to the
   component background, not physical road width.
2. Give foreground pixels row-major node IDs. Connect every 8-neighbor foreground
   pair, including diagonal-only contacts, matching existing label connectivity.
   No background nodes or gap bridging. Store a symmetric sparse CSR graph;
   there are at most eight directed neighbor entries per node.
3. Select endpoint candidates on the **geometric** graph, with edge length
   `ell = hypot(dx, dy)` (1 or sqrt(2)). Start at the lowest node ID, select the
   farthest reachable node A, then select the farthest from A as B. Distance ties
   within `rtol=1e-12, atol=1e-12` choose the lowest node ID. This is a two-sweep
   heuristic, not an exact diameter algorithm for general graphs.
4. Between A and B, route using symmetric positive costs
   `w(u,v) = ell(u,v) * (1 / edt(u) + 1 / edt(v)) / 2`.
   Endpoint selection uses geometric distance so thin pixels' larger interior
   costs do not themselves define what “farthest” means. The fixed cost is a
   hypothesis to test, not a tunable parameter sweep in CR-09.
5. Use single-source `scipy.sparse.csgraph.dijkstra(..., indices=source)` for each
   sweep and the weighted route. Never omit `indices`: no all-pairs matrices.
   Reconstruct from weighted distance labels to the target: choose the lowest-ID
   neighbor satisfying `d[current] ≈ w + d[neighbor]` with the tolerance above
   and strictly decreasing distance. This makes equal-cost choices explicit
   instead of relying on a version-specific predecessor tie. Orient the final
   path with the lower endpoint node ID first.
6. Keep every vertex, with no repeated consecutive vertices. Every step is an
   8-neighbor step in the same component. No four-pixel sampling, smoothing,
   skeleton pruning, or Douglas–Peucker in this change. State the known ceiling
   in a `ponytail:` comment: one route cannot encode branches or closed loops;
   use separately scoped topology extraction when that is needed.

Handle disconnected/empty masks, an unreachable endpoint, or an invalid
predecessor chain as an explicit `ValueError`. These violate the accepted-component
contract; do not return the old chord or silently drop a candidate. Tests exercise
the helper's narrow contract without promising to repair arbitrary malformed rasters.

The design uses O(B + V + E) storage for crop area B and component graph V/E,
and a fixed number of single-source searches. Record measured synthetic runtime
and largest component size; do not claim full-scene scalability from 256-pixel tiles.

API basis: SciPy's [Dijkstra documentation](https://docs.scipy.org/doc/scipy/reference/generated/scipy.sparse.csgraph.dijkstra.html)
defines single-source indices, warns about asymmetric weights with undirected
graphs, and notes version-dependent choices among equal solutions. Its
[EDT documentation](https://docs.scipy.org/doc/scipy/reference/generated/scipy.ndimage.distance_transform_edt.html)
defines distance to zero-valued background. The padding, symmetric cost, endpoint
heuristic, and tie policy above are this plan's design choices, not SciPy guarantees.

## Data and consumer contract

| Field or consumer | Required behavior |
| --- | --- |
| `path_px` | New field on every newly extracted candidate; ordered finite `[x, y]` pairs, at least two distinct vertices, in global raster coordinates |
| `path_length_px` | Sum of Euclidean lengths of consecutive path segments; raster path length, not meters or surveyed road length |
| `endpoints_px` | Original two-point PCA chord, retained as a legacy diagnostic |
| `length_px`, `width_px` | Original major/minor-axis component extents; neither changes meaning; `width_px` is not road width |
| Other existing fields | Preserve values, candidate IDs, order, count, and ranking; do not rank by the new length |
| `candidate_coordinates_px(candidate)` in `extract.py` | Shared selector used by export, scoring and plotting. Prefer present `path_px`; accept `endpoints_px` only when the key is absent. Validate numeric finite N×2 geometry, N≥2, and no consecutive duplicate points. Malformed present paths raise `ValueError`, including `None`, empty, NaN/Inf and wrong shape |
| `to_geojson` | Emit the selected path as one LineString; apply the supplied transform to **every** vertex. Omit both coordinate arrays from properties; retain scalar attributes. Endpoint-only legacy dictionaries continue to work |
| `rasterise_segments` | Keep the current function name/signature; rasterize every consecutive pair selected by the helper using the existing segment sampling and clipping convention |
| `score_lines` | Keep reference-centerline denominators, empty-truth `None`, and area coverage as a separate diagnostic |
| Demo | Plot the same selected coordinates; caption describes measured synthetic behavior and avoids implying branch recovery or real-road validation |

The current scoring band is **two iterations of four-neighbor dilation**, an L1
pixel tolerance, not a Euclidean two-pixel disk. Preserve that exact convention,
`tol_px=2`, and the existing `line_ok` cutoff of recall/precision ≥0.5. Do not
change a metric while comparing the old chord and new path.

## Acceptance and tests, frozen before implementation

These are proposed **software acceptance targets**, chosen before running this
algorithm. They are not observed results or an independent scientific preregistration.
All ten existing cases are development material. A failed target stays a failure.
The 0.80 target requires substantially better geometry than the existing coarse
0.50 `line_ok` label; neither threshold establishes real-road accuracy.

| ID | Required check |
| --- | --- |
| A1 — selection invariant | On all ten cases and the strength sweep, IDs, order, candidate count, false-candidate count, component recall/precision and every old scalar/chord match the baseline; retain the original scene fingerprint and endpoint tolerances |
| A2 — curved representation | `low_snr`, `tight_curve`, and `demo_reference` each reach line recall **and** precision ≥0.80 with the unchanged scorer; preserve before/after values and separate chord scores |
| A3 — straight preservation | Wide recall/precision each ≥0.98. Faint and gradient recall/precision each at least their own v3 value minus 0.02. These small allowances cover raster endpoint placement; no averaging across cases |
| A4 — geometry validity | Hand-built straight, diagonal-only, narrow U-turn, border-touching and non-square crops produce finite adjacent paths confined to the component; length equals the segment sum; repeated calls are identical. A one-pixel ring and a branched mask remain connected single paths without being called complete topology |
| A5 — exporter truth | With conflicting chord/path fields, GeoJSON and scorer use the path. A nontrivial transform reaches all vertices. Rasterized identity-transform GeoJSON coordinates equal the mask scored by `rasterise_segments` |
| A6 — discriminating controls | Corrupt only `path_px`: line scores deteriorate while component scores stay fixed. Corrupt only the chord when a valid path exists: delivered geometry and its scores stay fixed. Endpoint-only legacy fixtures retain the original wrong-diagonal, perfect-centerline, and offset-centerline controls |
| A7 — trust boundary | Parameterized invalid present paths raise `ValueError` through the shared selector and its consumers; no hidden chord fallback. Empty candidate lists still export/score normally |
| A8 — evidence integrity | v1 and archived v3 bytes remain unchanged; v4 CLI output includes schema/provenance metadata and reproduces the committed numeric payload; demo path coordinates are checked as well as the legacy chord |

The narrow U-turn must have both ends on the same side, unlike the current
`tight_curve` half-ellipse. Build its truth geometry directly, before routing;
assert the path stays on its one-pixel corridor and reaches both tips. A vertical
extent >15 px alone does not prove valid routing or that a path doubles back.
Include a wide straight strip to expose boundary-biased endpoints. The ring and
branch cases test safety/limits, not a new topology-recovery acceptance claim.

After a frozen implementation passes A1–A8, run the existing parameterized cases
`low_snr(seed=117)`, `faint_corridor(seed=111)`, and
`gradient_background(seed=118)` once. Retain all scores as synthetic sensitivity
checks, without tuning on them or calling them held-out site validation. If used
to revise the implementation, label them development data and disclose the revision.

## Work order and evidence migration

Budget: **one hour for routing feasibility, then approximately 2–4 hours for
integration and evidence if it passes**. Estimates are not time spent. Tasks below
are single-concern work units, normally 2–5 minutes of active work each; test and
environment waits are additional. Continue locally in dependency order.

Evidence destination for the future build: `evidence/task-2026-09-14/README.md`
(new). Do not create an implementation-success record in this plan PR.

| Task | Files / action | Depends on | Done when |
| --- | --- | --- | --- |
| T00 | Record actual merged base, clean status, Python/NumPy/SciPy versions, and baseline commands in the new evidence README | Plan merged | Baseline exits and any pre-existing failure are recorded |
| T01 | Archive `results/extractor_stress_cases.json` as new `results/extractor_stress_cases_baseline_v3_2026-09-14.json`; snapshot full baseline candidates as described below | T00 | Archive equals base v3 byte-for-byte; old candidate values and artifact hashes captured before edits |
| T02 | Add hand-built routing fixtures to new `analysis/tests/test_path_export.py` | T01 | Geometry exists independently of router output, including a true U-turn |
| T03 | Add A4 helper contract assertions in that test file | T02 | Tests fail because `_component_path_px` is missing; no fixture is adapted to its output |
| T04 | Implement crop padding, row-major nodes and symmetric sparse geometric edges inside `_component_path_px` | T03 | Graph has only foreground neighbors and diagonal lengths sqrt(2); review confirms sparse storage |
| T05 | Implement the two geometric endpoint sweeps | T04 | Tip selection succeeds on the one-pixel open fixtures; fixed `indices` used |
| T06 | Add symmetric interior costs and canonical distance-label reconstruction | T05 | A4 routing checks pass, including border, connectivity and repeated-run checks |
| T07 | Attach global `path_px` and `path_length_px` after existing acceptance, without changing selection or ranking | T06 | Old candidate fields match the captured baseline on all existing cases |
| T08 | Evaluate A2/A3 with the existing scorer by supplying each path's consecutive segments as temporary endpoint-only dictionaries | T07 | All proposed targets pass, or the feasibility stop below is recorded before further integration |
| T09 | Add shared-selector tests for preferred paths, legacy absence, and invalid present values | T08 passes | Tests fail for the missing contract; no broad silent fallback |
| T10 | Implement `candidate_coordinates_px` in `extract.py` | T09 | A7 selector tests pass |
| T11 | Add GeoJSON preference, every-vertex transform and property tests in `test_extract.py` | T10 | Current chord-only exporter fails new preference assertions |
| T12 | Route `to_geojson` through the selector | T11 | New export tests and endpoint-only legacy cases pass |
| T13 | Add polyline-vs-GeoJSON raster equality and corruption controls in `test_stress_cases.py` | T12 | Existing chord-only rasterizer fails path assertions |
| T14 | Update `rasterise_segments` to iterate selected consecutive pairs | T13 | A5/A6 pass without changing dilation, denominators or `line_ok` |
| T15 | Keep chord/width regression assertions; add A1–A3 path assertions | T14 | Old failures remain demonstrable using archived/legacy geometry; new path targets pass |
| T16 | Complete the CLI's v4 record dictionary in `stress_cases.py` | T15 | Temporary CLI output has the metadata and comparisons specified below |
| T17 | Regenerate v4 in a temporary file, validate it, then replace the current result record | T16 | All cases, sweep, chord comparison and acceptance results are retained, including failures |
| T18 | Extend record tests for complete schema, archive equality, and CLI numeric replay | T17 | A8 passes; metadata omission or a wrong exported path makes the checks fail |
| T19 | Extend `test_demo_reproducibility.py` with demo path/ID/order comparisons against v4 | T18 | The four old numeric tests remain intact; perturbing the new path is also detected |
| T20 | Switch `demo_synthetic.py` to the shared selector; correct caption; regenerate PNG from source | T19 | Visual inspection agrees with tested path geometry; no clipped text or unsupported recovery claim |
| T21 | Update `docs/figure-manifest.json`, `docs/data-and-figures.md`, `analysis/README.md` and `results/README.md` | T20 | New path provenance and scalar/compatibility semantics are documented; historical chord record remains labeled |
| T22 | Run the three declared sensitivity seeds; write the before/after table and mandatory failure taxonomy in the evidence README | T21 | Every attempted run, runtime, version, outcome and limitation is retained |
| T23 | Run integration checks; update only CR-09's ledger status; open the build PR citing the merged plan | T22 | All acceptance rows traced to evidence; no research gate relabeled |

T01 also writes new `evidence/task-2026-09-14/baseline_candidates.json`, containing
the complete ordered candidate dictionaries for all ten cases and each strength
in `faint_strength_sweep`. The existing v3 record has only aggregate scores and
rounded top-candidate summaries; it cannot prove A1 alone. Record the baseline
command, source revision, and SHA-256 of this snapshot, v1, v3, and the legacy
demo numeric record. Compare integer/list structure exactly and old floating
candidate fields with the existing 1e-6 absolute tolerance. Do not regenerate
this baseline with the new extractor.

V4 must include `schema_version=4`, `supersedes` naming the archived v3 and v1,
`geometry_field="path_px"`, extractor defaults, tolerance value/metric, generation
command, baseline revision, and Python/NumPy/SciPy versions. Keep existing case
metric keys for the new delivered path. Add a `chord_line` comparison per case
computed through the same scorer with endpoint-only candidate dictionaries; it
must reproduce the old line layer. Keep `area_coverage` separate.

In `cases.demo_reference`, add `exported_paths_px`, an ordered list of objects
containing candidate `id`, full `path_px`, and `path_length_px`. This supplies the
new demo geometry record without another generator or standalone path artifact.
The figure manifest points at v4 for rendered path geometry and explicitly retains
`method_demo_synthetic.numeric.json` as the legacy chord/scene record. Preserve
the old environment sidecar as historical; new runtime versions live in v4.

Record tests compare the complete numeric payload and required metadata fields;
environment strings may differ between supported machines and must be reported,
not overwritten to match. If tie behavior changes numeric results across versions,
investigate it before re-recording or widening tolerances. The current CLI emits
only cases/sweep/width diagnostics: schema v4 is incomplete until T16 fixes that.

## Verification and stop rules

For the **plan PR only**: parse the ledger with its existing schema; prove old
rows are byte-preserved and CR-09 occurs once; check relative links and source
references; run `git diff --check`. Only this document and one planned ledger row
change. No claim of passing a newly implemented path test is made.

For the **future build**, run from the repository root in an environment installed
with `python -m pip install -e "analysis[dev]"`. Capture actual versions and exits.
Use the documented [local readline workaround](START_HERE.md) only if needed and
record it. No separate typechecker or linter is configured.

```sh
python -m compileall -q analysis/catanroads analysis/tests
PYTHONPATH=analysis MPLBACKEND=Agg python -m pytest analysis/tests -q
node tools/validate_phase1.mjs
node tools/test_temporal_qa.mjs
PYTHONPATH=analysis python -m catanroads.site_worksheet --check
python tools/check_presentation.py . "Informal Road Mapping" informal-road-mapping
python tools/test_presentation.py
git diff --check
```

Run `PYTHONPATH=analysis python -m catanroads.stress_cases` to a **temporary file**,
then require exit 0, valid complete JSON, and the comparisons above before copying
it into `results/`. A CLI failure must not truncate the committed record. Replay
the committed record through the suite after replacement. Run
`MPLBACKEND=Agg python analysis/demo_synthetic.py` only at T20; inspect the image
and the provenance diff. PNG byte identity is not a gate.

**Feasibility stop:** after T08, or one hour of routing work, stop if A2/A3 or
geometry validity fails. Retain exact outputs and the frozen thresholds in the
evidence README; leave CR-09 incomplete. Do not silently substitute another
algorithm, relax 0.80/straight criteria, change connectivity or the two-pixel
metric, retune extractor defaults, or rewrite fixtures. A negative feasibility
result is a useful reviewable outcome, with the failed implementation kept out
of a completed-feature merge. Revise this plan prospectively if another method
is needed. Do not spend the remaining budget polishing a failed route.

**Completion stop:** CR-09 becomes `done` only after A1–A8 and integration pass,
with the build commit/PR and evidence linked. Unexpected component changes,
unexplained record drift, a consumer still emitting chords, or a new dependency
stop delivery until resolved. The synthetic branch/ring limitations and sensitivity
scores are reported even when the acceptance criteria pass.

## Remaining failures and the owner task

The build evidence must retain these limits in the same PR: crossings rejected
by component elongation; short dashes rejected by length; riverbank/field-edge
confounds still accepted; faint routes still fragmented; one route per accepted
component does not resolve junctions, loops, or braided-route topology. Improving
line geometry does not improve road/non-road discrimination by itself.

CR-08 stays blocked on actual dated imagery judgments and Earth Engine inputs.
The smallest next input remains one development/control image with the fields in
[SITE_VERIFICATION_WORKSHEET.md](SITE_VERIFICATION_WORKSHEET.md). A rejected or
uncertain judgment is retained, not converted to `verified=true`. Follow
[COMPLETION_RECONCILIATION.md](COMPLETION_RECONCILIATION.md) and
[PHASE1_RUNBOOK.md](PHASE1_RUNBOOK.md); preserve the untouched holdout. CR-09 can
proceed on synthetic inputs and cannot close this research gate.

Junction recovery, gap bridging, width estimation, network conditioning, optional
geospatial dependencies, broad documentation cleanup and historical replay
re-pinning remain separate tasks. The old replay is historical evidence; this
plan does not newly assert that it was rerun or failed.

## Requirement traceability

| Requirement | Tasks |
| --- | --- |
| A1 — unchanged detection and legacy data | T00–T01, T07, T15, T18 |
| A2/A3 — measurable representation improvement | T08, T15, T17, T23 |
| A4 — bounded, valid representative path | T02–T08 |
| A5 — same geometry in every consumer | T11–T14, T19–T21 |
| A6/A7 — discriminating tests and explicit invalid input | T09–T15 |
| A8 — reproducible new evidence and preserved history | T01, T16–T23 |
| Remaining failures and CR-08 boundary | T22–T23 |
